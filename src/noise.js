
function sumOctaves(iteration, x, y, persistence, scale, low, high) {
    let maxAmplitude = 0;
    let amplitude = 1;
    let frequency = scale;
    let noise = 0;

    for(i = 0; i < iteration; i++) {
        noise
    }
}


export default Noise(x,y,scale) {
    let arr = []
    for (let i = 0; i < x; i++) {
        for (let j = 0; j < y; j++) {
            
        }
    }
}